services:
  test:
    build: .
    volumes:
      # Mount source and tests so edits are reflected without rebuild
      - ./src:/app/src
      - ./tests:/app/tests
    environment:
      - PYTHONPATH=/app/src
    command: pytest
