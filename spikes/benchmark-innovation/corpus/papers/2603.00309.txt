Title: DIG to Heal: Scaling General-purpose Agent Collaboration via Explainable Dynamic Decision Paths
Authors: Hanqing Yang, Hyungwoo Lee, Yuhang Yao, Zhiwei Liu, Kay Liu et al.
Published: 2026-02-27
Source: https://arxiv.org/abs/2603.00309
ArXiv ID: 2603.00309

DIG to Heal: Scaling General-purpose Agent Collaboration via
Explainable Dynamic Decision Paths
Abstract
The increasingly popular agentic AI paradigm promises to harness the power of multiple, general-purpose large language model (LLM) agents to collaboratively complete complex tasks. While many agentic AI systems utilize predefined workflows or agent roles in order to reduce complexity, ideally these agents would be truly autonomous, able to achieve emergent collaboration even as the number of collaborating agents increases. Yet in practice, such unstructured interactions can lead to redundant work and cascading failures that are difficult to interpret or correct. In this work, we study multi-agent systems composed of general-purpose LLM agents that operate without predefined roles, control flow, or communication constraints, relying instead on emergent collaboration to solve problems. We introduce the Dynamic Interaction Graph (DIG), which captures emergent collaboration as a time-evolving causal network of agent activations and interactions. DIG makes emergent collaboration observable and explainable for the first time, enabling real-time identification, explanation, and correction of collaboration-induced error patterns directly from agents’ collaboration paths. Thus, DIG fills a critical gap in understanding how general LLM agents solve problems together in truly agentic multi-agent systems. The project webpage can be found at: https://happyeureka.github.io/dig.
1 Introduction
Large Language Models (LLMs) are increasingly used as general decision-making units due to their broad world knowledge, flexible reasoning abilities, and capacity to interact with external tools and memory. This has led to the emergence of LLM agents, which extend LLMs with persistent state, tool access, and environment interaction, enabling them to act autonomously in complex tasks. Recently, significant attention has been devoted to LLM-powered multi-agent systems (Chen et al., 2026; Yang et al., 2025), where multiple agents collaborate on tasks such as research, software engineering, robotics, simulation, and question answering (Durante et al., 2024).
Despite the potential of allowing LLM agents to act autonomously, in practice unstructured agent collaboration often leads to task failure. Thus, many multi-agent systems utilize predefined workflows, with fixed roles, task decompositions, tool usage patterns, and message routing policies, in order to stabilize performance (Li et al., 2024). However, such designs are often domain-specific and fundamentally constrain how agents interact. In contrast, we study LLM-powered multi-agent systems composed of general-purpose LLM agents that operate without predefined roles, control flow, or communication constraints. In such systems, coordination, task decomposition, and problem-solving strategies must arise implicitly from local agent decisions and interactions, resembling how human teams dynamically adapt to evolving tasks. Our goal is to characterize analogous forms of cooperative intelligence in general-purpose LLM agents and illuminate the causes of collaboration failures so as to enable more resilient autonomous multi-agent systems.
The Challenges of Emergent Cooperation. Allowing agents to autonomously interact introduces substantial challenges. Each agent receives information, from other agents and the environment, at arbitrary times while acting concurrently with others. Local agent decisions can thus easily push global execution away from intended behavior, producing redundant work, stalled progress, or cascading failures that are difficult to diagnose or recover from. Simply increasing the number of agents does not guarantee improved performance; instead, interaction complexity grows combinatorially, leading to even more interdependent outputs that may cause task failures or redundancies. Unlike traditional distributed systems, multi-LLM agent systems are stochastic, heterogeneous, and message-driven, making tracing, debugging, and recovery particularly challenging. Existing approaches typically rely on centralized monitors (Gandhi et al., 2025; Zhang et al., 2025a), guard agents (Yu et al., 2025), or post hoc failure attribution based on execution logs or curated error datasets (Kong et al., 2025; Zhang et al., 2025b). These methods either assume fixed workflows, operate after failures have already occurred, or require large amounts of annotated data, and therefore do not directly address how cooperation emerges or fails during execution.
Our Perspective: Analyzing Agent Interactions. In this work, we argue that understanding and improving general multi-agent systems requires treating collaboration itself as the primary object of analysis. Rather than focusing on individual agents or final outcomes, we model the system in terms of its interaction structure. We introduce the Dynamic Interaction Graph (DIG), a time-evolving directed graph that represents agent activations and interaction events as a causal network. This abstraction makes emergent collaboration observable and explainable, enabling real-time tracing, structural analysis, and principled reasoning about how cooperative behavior forms, propagates, and fails.
Building on DIG, we establish a topological characterization of emergent collaboration, which maps interaction graph structures to distinct classes of collaboration-induced error patterns. This result provides a model-agnostic categorization of failure modes, enabling early failure detection at the level of collaboration structure rather than individual agent outputs. Importantly, this structural perspective is designed to be compatible with existing failure detection and attribution methods (Cemri et al., 2025). Rather than replacing them, it provides an error detection and guarding layer, where topology-level signals can be integrated into semantic, behavioral, and learning-based detectors. The DIG framework further supports reasoning about effective agent utilization, revealing when additional agents contribute meaningful progress versus when they introduce redundancy, inefficiency, or instability. Together, these capabilities provide a unified and explainable foundation for understanding and debugging truly agentic multi-LLM systems, where cooperation emerges dynamically rather than being imposed by predefined workflows.
Our main contributions are: (1) Interaction Modeling. We formalize the interaction structure and cooperative problem-solving process of general LLM agents with DIG, a dynamic directed acyclic graph (DAG) representation that captures both sequential and parallel dependencies, as well as temporal ordering, in unconstrained multi-agent systems. (2) Topological Failure Characterization. We establish a principled mapping from the DIG topology to collaboration-induced error types, yielding a high-level, model-agnostic categorization of failure modes that supports explainable and hierarchical error detection and can be naturally integrated with existing error detection and attribution methods. (3) General-Agent Execution Traces. We evaluate DIG on two scalable and verifiable evaluation tasks, allowing us to introduce the first dataset of execution traces that capture the behavior and interaction patterns of general-purpose agents operating without predefined roles or control flow. (4) Structure-Driven System Healing. We propose an automatic system healing mechanism driven by observed agent behavior and interaction structure, enabling early detection and correction of collaboration-induced failures during execution. In experiments, we find that this mechanism yields improved efficiency and fewer errors, particularly on difficult tasks with many agents
After reviewing related work in Section 2, we formalize agent interactions and DIG’s construction in Section 3. We then explain how DIG allows for failure monitoring and interventions in Section 5. We then describe the design (Section 6) and results (Section 7) of our validation experiments and conclude in Section 8.
2 Related Work
LLM-powered Multi-agent Systems. Recent frameworks such as LangGraph (lan, ), AutoGen (Wu et al., 2023), and AgentVerse (Chen et al., 2023) enable multi-agent collaboration through structured workflows and orchestration policies, and have been applied to tasks including research assistants, software engineering, robotics, and simulation (Chen et al., 2026). While effective in domain-specific settings, these systems rely on predefined roles, control flow, and communication structures, and thus provide limited insight into emergent cooperation in unconstrained, general-agent systems.
Agent Interaction Modeling. Our interaction model is related to classical abstractions such as stochastic computation graphs (SCGs) (Schulman et al., 2016), Petri nets (Peterson, 1977), and the Actor model (Agha, 1986), which represent computation and concurrency through graph structures. However, unlike these formalisms, LLM-based multi-agent systems are message-driven, asynchronous, and rely on open-ended, stochastic communication in natural language without fixed protocols. Our work extends these abstractions by providing a lightweight interaction model tailored to emergent collaboration in multi-LLM systems.
Failure Characterization in Multi-agent Systems. Recent taxonomies show that multi-agent LLM systems frequently fail due to specification errors, misalignment, and verification breakdowns, with local failures often cascading into system-wide issues (Cemri et al., 2025). Prior work addresses these failures through post hoc attribution (Kong et al., 2025; Zhang et al., 2025b), centralized monitors or guard agents (Yu et al., 2025; Gandhi et al., 2025; Zhang et al., 2025a), and runtime supervision mechanisms (Wang et al., 2025; Lin et al., 2025), as well as graph-based monitoring and defense strategies (Wu et al., 2025). While effective in specific settings, these approaches are typically coupled to fixed agent architectures or orchestration patterns, rely on curated error datasets or centralized components, and diagnose failures at the level of individual agents or final outcomes. By contrast, our work provides a lightweight, framework-agnostic tracing system that captures interaction dynamics among general agents, enabling systematic and explainable error attribution and scaling naturally to massively parallel multi-agent systems.
3 Problem Formulation: Cooperative Problem Solving with General Agents
We consider cooperative problem solving in systems of multiple general-purpose agents that interact asynchronously via message passing, with no predefined roles, control flow, or communication protocols. Coordination and task decomposition emerge solely from local agent decisions, as shown in Figure 1.
System Model. A cooperative multi-agent system is characterized by the tuple
where is a finite set of agents, is the set of interaction events, denotes a problem instance, and denotes discrete logical time. The problem instance is encoded as a distinguished initial event , delivered at to a subset . Execution terminates when a terminal event is generated. Logical time advances whenever an interaction occurs, either through agent activation or event delivery. It induces a causal ordering over interactions but may not correspond to wall-clock time.
Each agent is modeled as an autonomous decision-making unit that operates asynchronously and has no predefined role or control flow. Thus, we abstract away agent internals and model only observable interaction behavior via a non-deterministic transformation on events
Each agent maintains a local event buffer . Let denote whether is activated at time . Activation occurs whenever the buffer changes while the agent is idle . The buffer may change if the agent receives a new event (see the description of events below), or if the agent acts on an event which is then cleared from the buffer. For instance, the event may correspond to a computational task, like sorting a list, which the agent can then complete while it is activated.
Agents interact through events , which are defined as
where is the payload and is a delivery policy. For example, can be the result of a computation completed by an agent , whose delivery policy is to send the result immediately to all other agents . At each logical time , determines the set of recipients and an updated policy . The event is appended to the buffers of agents in . If is empty, the event expires.
Tracking Agent Dynamics. The coupled dynamics of agent transformations (2) and policy-driven event delivery (3) induce a stochastic execution process over logical time. An execution trace is then defined as
where is the activated agent at time (if any), and is the event generated or delivered at time .
Due to asynchrony and non-determinism, system behavior is not fully characterized by the terminal output . Failures may arise from intermediate interaction structure, such as stalled activations, lost subproblems, duplicated work, or premature termination. We therefore study the following inference problem: given only an observable execution trace , infer a system-level diagnosis function
where denotes a space of failure signals and explanations. For example, a failure signal might consist of a computation task (i.e., an event) being forwarded indefinitely between agents, none of whom complete the task and generate the result. The function must operate solely on observable interaction structure, without access to agent internals, semantic task labels, or outcome supervision. To solve this problem, in the next section, we introduce the Dynamic Interaction Graph (DIG), a formal execution representation of agent interactions in cooperative problem solving.
4 Method: Dynamic Interaction Graph (DIG)
We present the Dynamic Interaction Graph (DIG) as a system-level representation and inference framework for cooperative problem solving in general multi-agent systems. Concretely, our method takes an execution trace as input, constructs a labeled causal interaction graph , and performs graph-level inference to detect failures, explain coordination dynamics, and derive interventions using only observable interaction structure from .
4.1 DIG Definition
We represent cooperative multi-agent execution as a directed, dynamic, bipartite causal DIG graph. At logical time , the system induces a graph
where is the set of activation nodes, is the set of event nodes, and is the set of directed edges, as illustrated in Figure 2(1) and (2). The graph is bipartite: edges connect only activation nodes to event nodes or event nodes to activation nodes. Each activation node corresponds to the activation of an agent that lasts from time to :
Each event node corresponds to an interaction event with realized delivery structure:
Edges. Two edge types exist: (1) Generation edges , which exist if generates ; and (2) Delivery edges , which exist if triggers activation . Collectively, defines a complete temporal causal trace of execution. All causal influence propagates exclusively through these edges.
Illustration.
Figures 2(4) and (5) illustrate the DIG. Each circle denotes an activation node , spanning , and each square denotes an event node at generation time . Edges from circles to squares represent generation , while edges from squares to circles represent delivery . Figure 2(4) shows the full real-time DIG, including transient edges, and Figure 2(5) shows the cleaned graph after removing non-productive edges. In Section 4.2, we discuss the distinction between transient and productive edges. Together, they illustrate how an execution trace maps to a time-indexed bipartite causal graph , where coordination structure is encoded by nodes and directed edges. DIG thus provides a complete system-level representation of cooperative multi-agent execution, making coordination dynamics and causal dependencies observable.
4.2 Interaction Primitives as Graph Rewrite Operators
The DIG evolves through local graph rewrite operators induced by agent activations. When an agent is activated at logical time , the activation is represented by an activation node . Let denote the local event buffer of agent at time , and let denote the subset of events selected by the agent during the activation. These events correspond exactly to the set of incoming delivery edges of the activation node:
We further assign a causal fate to each incoming interaction edge by defining a system-level edge rewrite semantics through a mapping . This induces a local graph rewrite operator that transforms the DIG by modifying, removing, or creating nodes and edges. We elaborate these operators next.
Canonical Rewrite Operators. Let denote the set of new events generated by the agent during the activation, which correspond to newly created event nodes in the DIG. The standard interaction primitives correspond to the following canonical edge rewrite patterns:
(1) Respond (R): The agent acts on all buffered events, and , and labels all incoming edges as consumed, . The DIG is rewritten by removing all input events from the agent buffer and generating new event nodes,
(2) Wait (W): The agent preserves its buffer, , and labels all incoming edges as delayed, . No graph modification is applied: .
(3) Reroute (RR): The agent forwards selected events, , and labels their edges as rerouted, . Each such edge is redirected:
(4) Discard (D): The agent drops selected events, , and labels the edges as discarded, . The edges are removed:
(5) Submit (S): A terminal Respond with , yielding
Global Execution Semantics. The overall system execution is the composition of local graph rewrite operators induced by activation nodes:
where each is determined by the edge-action labeling function .
Edge Attribution. We attribute each edge as either productive or non-productive. Non-productive edges denote interactions that do not advance the cooperative solution at the current step. Edges corresponding to Wait (W) and Discard (D) are non-productive and drawn as dashed, indicating that no productive work is performed at that time. For Reroute (RR), the edge from the source agent is non-productive and dashed, while the redirected edge to the final recipient is productive and solid. Figure 2(3) and (4) illustrate this distinction, with solid lines representing productive edges and dashed lines representing non-productive edges.
Illustration.
Thus, cooperative multi-agent execution is formally a graph rewriting dynamical system. As illustrated in Figure 2(3), agents do not coordinate through predefined roles or control flow, but by inducing local transformations on the edges of the DIG. All emergent cooperative structure, system-level behavior, and failure modes arise from the composition of these local edge-level rewrite operators, making DIG the effective state space of cooperative intelligence.
4.3 DIG as a Representation of Cooperative Intelligence
In this section, we argue that our DIG representation is not a visualization artifact but an intermediate state space on which we can perform system-level reasoning. We therefore treat the DIG as the representation of cooperative intelligence itself.
DIG as a Labeled Causal Interaction Graph. For any logical time , let
where is the directed bipartite causal topology defined in Section 4.1, and is the edge-action labeling induced by local rewrite operators (Section 4.2). Thus, each is a fully labeled causal interaction graph, encoding both who interacted with whom and how each interaction edge was operationally treated.
We can further use this formalism to characterize agents’ cooperative behavior by defining the cooperative behavior induced by an execution trace as the equivalence class
where denotes isomorphism of the time-indexed labeled graphs , preserving (i) bipartite node types, (ii) directed adjacency, and (iii) edge labels in . This defines cooperative behavior purely in terms of observable interaction structure, independent of agent identities, internal states, prompts, or architectures. Cooperative behavior is therefore encoded in the sequence of graph transformations applied to the DIG:
That is, cooperation is fully characterized by the temporal evolution of the interaction graph under the edge-level rewrite operators . Each specifies how interaction edges are locally consumed, delayed, rerouted, or discarded, and therefore makes the system’s coordination dynamics explicitly observable.
DIG as an Explainability Trace. Consequently, the sequence constitutes an explainability trace of cooperative intelligence. Any system-level behavior or failure signal inferred from an execution trace satisfies
for some graph-level inference operator . Thus, explanations depend only on the labeled causal structure and are invariant to all unobservable agent internals. In this sense, DIG is not merely a representation of execution, but an explicit causal explanation object for emergent multi-agent collaboration.
Theorem 4.1 (Topological Inference Reduction).
Fix a class of cooperative multi-agent systems whose observable semantics are defined by asynchronous activations and event passing as in Section 3. Let be the space of observable execution traces and let be the DIG construction operator.
For any system-level functional that depends only on observable interactions (equivalently, is invariant over the equivalence classes in (12)), there exists a graph functional such that
The proof of Theorem 4.1 can be found in Appendix A. This result establishes that all structurally observable system-level reasoning about cooperative intelligence reduces to inference over the DIG. In other words, diagnosis, explanation, and intervention are not operations over agents, but over the topology and edge-action structure of the evolving causal interaction graph itself.
4.4 Problem-Solving in DIG
We interpret cooperative problem solving as a structural transformation process over the DIG. Given an execution trace and its induced DIG sequence , task progress is realized through the composition of local graph rewrite operators (Eq. (10)), where each activation node induces a rewrite through its edge-action operator and output event set , rather than through access to agent internals.
Each activation node consumes an input set of events and produces an output set of events, as recorded by the incident edges in the DIG. Let
denote the input events delivered to and the output events generated by , respectively. We interpret as a local problem-solving transformation .
An activation is problem-generating if it expands the frontier of unresolved work, , in which case each is treated as a subproblem spawned from context . An activation is problem-reducing if it does not expand the frontier, , in which case each is treated as a subsolution produced from context .
5 Failure, Detection, and Healing
The system defines errors not by the Internal Reasoning of the agents, but by observable Interaction Primitives (actions taken by agents) and the structural invariants they violate in DIG.
Failure Taxonomy. Empirically, cooperative general agent systems exhibit failures that arise not from isolated reasoning mistakes within individual agents, but from breakdowns in interaction structure and execution dynamics. Table 1 summarizes the major classes of failures observed in our experiments with DIG.
The taxonomy is defined in terms of observable execution structure, without access to agent internals, task semantics, or predefined workflows. We consider two distinct classes of violated structural or temporal invariants: failures in preserving and exhausting work (Reachability and Coverage) and warnings indicating inefficient or risky handling of emitted work (Progress). This separation enables precise monitoring, diagnosis, and intervention in emergent cooperative problem solving. Moreover, the detection method remains task- and domain-agnostic, as it operates on the interaction structure.
Failure Detection and Healing. Whenever a new event is generated at time , the system temporarily blocks its delivery and evaluates the current for error patterns defined in Table 1 and shown in Figure 3. When an event is blocked due to a failure detection, the system may (i) inject new information into it before delivery, (ii) inject and reroute it to a recipient set, or (iii) create a new event and deliver it to a recipient set in order to heal from the failure. We detail how each failure pattern from Table 1 is detected and healed:
(1) ET. Detection: A Submit event is generated despite unresolved reachable work. Formally, and there exists without a directed path from to . Healing: apply (ii) by injecting information about unresolved events and rerouting it to the agent that issued Submit.
(2) MC. Detection: All reachable work is exhausted, but no Submit event is generated. Formally, and for exceeding a threshold. Healing: apply (i) by injecting information that all reachable work is exhausted.
(3) OE. Detection: An event is generated but has no recipients for longer than a reasonable time window. Formally, there exists , , such that its recipient set is empty, for longer than a reasonable time window. Healing: apply (ii) by injecting status information and rerouting the event back to its generating agent.
(4) DL. Detection: Reachable work remains, but no activation occurs for longer than a reasonable time window. Formally, while for all within a reasonable time window. Healing: apply (iii) by creating a new event and broadcasting it to all agents to restart activity.
(5) ER. Detection: An event is repeatedly Rerouted across activations without ever being Consumed. Formally, there exists such that its delivery edges are rerouted more than a reasonable number of times. Healing: apply (i) by injecting information indicating repeated rerouting.
(6) CLA. Detection: Events from different problem-generating activations are delivered to the same recipient agent. Formally, there exists and such that there is no activation whose generated events have a directed path to both and . Healing: apply (i) by injecting ancestry information into each event.
(7) RSP. Detection: Multiple problem-reducing activations Consume the same upstream event . Formally, there exist and problem-reducing activations such that . Healing: apply (i) by injecting information on which part of the result may be repeated.
Note that these depend entirely on agent interaction patterns as encoded in the DIG, allowing us to improve general problem-solving performance without modifying agents’ internal mechanisms or constraining their interactions.
6 Experiment Design
We evaluate DIG’s ability to reveal cooperative problem solving in two problem domains: (1) Count Frequency (CF), which requires computing the frequency of each element in an input integer array, and (2) 20 Newsgroups Classification (NC) (Crawford, ), which requires computing the frequency over the 20 topic categories for a set of documents. Both domains benefit from parallel reasoning and decentralized interaction, since the overall task can be decomposed into sub-tasks that can be explored or verified independently. For each domain, we define three difficulty levels based on input size: for CF domain, arrays contain 1000, 5000, or 10000 elements; for 20 NC domain, we use sets of 60, 100, or 150 documents. Across domains, we evaluate performance along two dimensions, accuracy and efficiency. Accuracy is measured using root mean squared error (RMSE) between the generated frequency values and the ground-truth frequencies for both Count Frequency and 20 Newsgroups. Efficiency captures end-to-end wall-clock problem-solving time. We further track the number of detected errors of each type.
Baselines. We compare DIG against two baselines: (1) MAS-only, which does not attempt to heal from failures; and (2) MAS+LLM Judge follows prior work (Cemri et al., 2025) by invoking an LLM to diagnose failures using a predefined error taxonomy, typically after a task has failed. Since it is not designed for real-time monitoring, we adapt it by running the judge periodically during execution every activations using our error taxonomy (see Appendix C). We use DIG’s failure detection to track and record failures and to construct the DIG. In our results, we refer to DIG as ‘MAS+Our Method’, which additionally uses DIG’s healing framework as described in Section 5.
7 Experiment Results
We first analyze the results of the DIG on both CountFrequency and 20Newsgroups and then present a case study with 20 agents to demonstrate DIG’s scalability.
7.1 Key Observations
Overview.
Table 2 reports total error counts, prediction accuracy (measured by RMSE), runtime, and valid output rate across all baselines, both domains, three difficulty levels, and 1, 3, and 6 agents. Detected Error and RMSE are averaged over three runs, while Valid Output Rate is averaged over ten runs. Beyond aggregate performance metrics, DIG also exposes the internal coordination structure of each experiment: full interaction graphs and agent activation timelines, enabling direct analysis of decentralized cooperation dynamics, are provided in Appendix D. Figures 6, 6, and 6 show representative DIG execution traces for cooperative problem solving at different scales. MAS+DIG maintains coherent causal structure from small (three-agent) to large (twenty-agent) systems, while MAS+LLM Judge exhibits increasingly fragmented and unstable interaction patterns as system size grows.
Reliability at Scale.
MAS+DIG generally obtains the lowest RMSE and higher valid output rates in Table 2, particularly for the hard tasks, indicating that it produces more accurate results. Detected error counts and runtime may be higher for MAS+DIG because it surfaces more observed errors in Table 2: healing keeps the system running longer, exposing additional failures and triggering further healing steps. In contrast, MAS-Only can appear faster with fewer detected errors due to the absence of intervention, which may leave failures unresolved as shown by lower valid output rates. As agent count and task difficulty increase, DIG more frequently enables the system to complete with valid and more accurate results, possibly at the cost of detecting more errors and running the system longer.
LLM Judge Overhead and Healing.
The LLM Judge baseline can incur high healing overhead, making real-time detection and healing impractical. The purple block in the bottom plot of Figure 6 illustrates the real-time overhead introduced by LLM judging. Despite this additional cost, MAS+LLM Judge often yields higher RMSE and lower valid output rates, indicating that not every intervention improves system performance. In contrast, DIG-based interventions are generally more effective, improving solution quality and reliability while incurring negligible overhead.
7.2 Case Study with 20 Agents
We conducted a 20-agent case study on the Count Frequency task with 100,000 elements (10 times our “hard” setting) to demonstrate the DIG’s value in highly complex settings. In our case study, the baseline system (MAS) fails to produce any result within 120 seconds without DIG-based healing, whereas our method enables the same 20-agent setup to produce a valid answer within 70 seconds. In this run, our method achieves an RMSE of 5812.69 (approximately 5.8% relative to the array size) with 92 calls, using 435,105 prompt tokens and 10,567 completion tokens.
Figure 6 shows the DIG generated by DIG (top), as well as times at which each agent and the system is activated (bottom). As we would intuitively expect in highly parallel settings, we observe some common interaction failures, including 1 missing completion error, 8 repeated-effort incidents, 2 dependency warnings, 1 orphaned event, and 4 early termination errors. Overall, these observations highlight that multi-agent collaboration is non-trivial: as the number of agents increases, the system becomes more susceptible to interaction failures, making scalable reliability a central challenge. DIG addresses this challenge by providing an agent-count-, agent-type-, and problem-domain-agnostic method that supports healing and improves system performance even with 20 agents operating in parallel.
8 Conclusion
In this paper, we introduce DIG, which uses the Dynamic Interaction Graph for multi-agent collaboration. DIG models emergent collaboration as a time-evolving causal network of agent activations and interactions, making collaborative dynamics observable and explainable. To the best of our knowledge, this is the first framework enabling real-time identification, interpretation, and correction of collaboration-induced error patterns directly from agents’ interaction paths. We view DIG as an open framework and anticipate continued community contributions to further strengthen and extend it, e.g., further evaluating scalability to more agents and more problem domains.
Acknowledgments
This work was supported in part by the Office of Naval Research under grant N000142412073 and the National Science Foundation under grants CNS-2312761 and CNS-2533813.
Impact Statement
This paper presents work aimed at advancing the field of machine learning by improving the interpretability, robustness, and reliability of general-purpose multi-agent systems. By making emergent agent interactions observable and explainable, our approach has the potential to support safer, more transparent, and more controllable deployment of agentic AI systems in practice. We do not identify ethical concerns beyond those commonly associated with large language model–based systems.
References
- (1) Langgraph. https://www.langchain.com/langgraph.
- Agha (1986) Agha, G. Actors: a model of concurrent computation in distributed systems. MIT press, 1986.
- Cemri et al. (2025) Cemri, M., Pan, M. Z., Yang, S., Agrawal, L. A., Chopra, B., Tiwari, R., Keutzer, K., Parameswaran, A., Klein, D., Ramchandran, K., et al. Why do multi-agent llm systems fail? arXiv preprint arXiv:2503.13657, 2025.
- Chen et al. (2026) Chen, J., Yang, H., Liu, Z., and Joe-Wong, C. The five ws of multi-agent communication: Who talks to whom, when, what, and why - a survey from MARL to emergent language and LLMs. Transactions on Machine Learning Research, 2026. ISSN 2835-8856. URL https://openreview.net/forum?id=LGsed0QQVq. Survey Certification.
- Chen et al. (2023) Chen, W., Su, Y., Zuo, J., Yang, C., Yuan, C., Qian, C., Chan, C.-M., Qin, Y., Lu, Y., Xie, R., et al. Agentverse: Facilitating multi-agent collaboration and exploring emergent behaviors in agents. arXiv preprint arXiv:2308.10848, 2(4):5, 2023.
- (6) Crawford, C. 20 newsgroups. https://www.kaggle.com/datasets/crawford/20-newsgroups.
- Durante et al. (2024) Durante, Z., Huang, Q., Wake, N., Gong, R., Park, J. S., Sarkar, B., Taori, R., Noda, Y., Terzopoulos, D., Choi, Y., et al. Agent ai: Surveying the horizons of multimodal interaction. arXiv preprint arXiv:2401.03568, 2024.
- Gandhi et al. (2025) Gandhi, S., Tsay, J., Ganhotra, J., Kate, K., and Rizk, Y. When agents go astray: Course-correcting swe agents with prms. arXiv preprint arXiv:2509.02360, 2025.
- Kong et al. (2025) Kong, F., Zhang, R., Yin, H., Zhang, G., Zhang, X., Chen, Z., Zhang, Z., Zhang, X., Zhu, S.-C., and Feng, X. Aegis: Automated error generation and attribution for multi-agent systems. arXiv preprint arXiv:2509.14295, 2025.
- Li et al. (2024) Li, X., Wang, S., Zeng, S., Wu, Y., and Yang, Y. A survey on llm-based multi-agent systems: workflow, infrastructure, and challenges. Vicinagearth, 1(1):9, 2024.
- Lin et al. (2025) Lin, F., Chen, S., Fang, R., Wang, H., and Lin, T. Stop wasting your tokens: Towards efficient runtime multi-agent systems. arXiv preprint arXiv:2510.26585, 2025.
- Peterson (1977) Peterson, J. L. Petri nets. ACM Computing Surveys (CSUR), 9(3):223–252, 1977.
- Schulman et al. (2016) Schulman, J., Heess, N., Weber, T., and Abbeel, P. Gradient estimation using stochastic computation graphs, 2016. URL https://arxiv.org/abs/1506.05254.
- Wang et al. (2025) Wang, Q., Wang, T., Tang, Z., Li, Q., Chen, N., Liang, J., and He, B. Megaagent: A large-scale autonomous llm-based multi-agent system without predefined sops. In Findings of the Association for Computational Linguistics: ACL 2025, pp. 4998–5036, 2025.
- Wu et al. (2025) Wu, C., Zhang, Z., Xu, M., Wei, Z., and Sun, M. Monitoring llm-based multi-agent systems against corruptions via node evaluation. arXiv preprint arXiv:2510.19420, 2025.
- Wu et al. (2023) Wu, Q., Bansal, G., Zhang, J., Wu, Y., Zhang, S., Zhu, E., Li, B., Jiang, L., Zhang, X., and Wang, C. Autogen: Enabling next-gen llm applications via multi-agent conversation framework. arXiv preprint arXiv:2308.08155, 2023.
- Yang et al. (2025) Yang, H., Chen, J., Siew, M., Lorido-Botran, T., and Joe-Wong, C. Llm-powered decentralized generative agents with adaptive hierarchical knowledge graph for cooperative planning, 2025. URL https://arxiv.org/abs/2502.05453.
- Yu et al. (2025) Yu, C., Lu, S., Zhuang, C., Wang, D., Wu, Q., Li, Z., Gan, R., Wang, C., Hou, S., Huang, G., Yan, W., Hong, L., Xue, A., Wang, Y., Gu, J., Tsai, D., and Lin, T. Aworld: Orchestrating the training recipe for agentic ai, 2025. URL https://arxiv.org/abs/2508.20404.
- Zhang et al. (2025a) Zhang, G., Wang, J., Chen, J., Zhou, W., Wang, K., and Yan, S. Agentracer: Who is inducing failure in the llm agentic systems? arXiv preprint arXiv:2509.03312, 2025a.
- Zhang et al. (2025b) Zhang, S., Yin, M., Zhang, J., Liu, J., Han, Z., Zhang, J., Li, B., Wang, C., Wang, H., Chen, Y., et al. Which agent causes task failures and when? on automated failure attribution of llm multi-agent systems. arXiv preprint arXiv:2505.00212, 2025b.
margins, page numbering, etc.) should be kept the same as the main body.
Appendix A Theoretical Results
A.1 Proof for Theorem. 4.1
Proof.
Because depends only on observable interactions, it is constant on each behavioral equivalence class induced by DIG isomorphism. Concretely, for any ,
Define on the range of by selecting any trace such that and setting
This definition is well-defined: if is another trace with , then holds trivially, so by Eq. (16) we have , and hence Eq. (17) does not depend on the choice of representative trace.
Finally, for any ,
where the second equality follows from the definition of in Eq. (17). Therefore . ∎
Appendix B General Agent
Appendix C LLM Judge
Appendix D Observing LLM Agents’ Decision Paths (Example Traces)
agents
agents
agents
agents
agents
agents
agents
agents
agents
Left: MAS-only, Middle: LLM-Judge, Right: Our-Method
D.1 Failure Case Analysis
Each agent routes their result to others and misses the opportunity to aggregate.
Appendix E Table
Table 3 summarizes the full structural error taxonomy, RMSE, and efficiency for easy tasks. The results show that increasing agent count introduces non-trivial coordination failures even under low task difficulty, and that DIG-based monitoring and healing significantly improves reliability compared to both MAS-only and MAS+LLM Judge.
Table 4 summarizes the full DIG error taxonomy and performance metrics for medium-difficulty tasks. Compared to the easy setting, coordination failures grow rapidly with agent count, particularly for MAS-only and MAS+LLM Judge. In contrast, our DIG-based method consistently reduces terminal failures and achieves lower RMSE with significantly higher efficiency, indicating more effective structural coordination under increased task complexity.
Table 5 summarizes DIG-based failure diagnostics and performance on hard tasks. Baseline MAS and MAS+LLM Judge suffer from frequent termination failures and highly unstable accuracy as agent count grows, while the DIG-based method consistently preserves causal structure and achieves more reliable outcomes under high difficulty.