Title: Structured Cooperative Multi-Agent Reinforcement Learning: a Bayesian Network Perspective
Authors: Shahbaz P Qadri Syed, He Bai
Published: 2025-10-11
Source: https://arxiv.org/abs/2510.09937
ArXiv ID: 2510.09937

Structured Cooperative Multi-Agent Reinforcement Learning: a Bayesian Network Perspective
Abstract
The empirical success of multi-agent reinforcement learning (MARL) has motivated the search for more efficient and scalable algorithms for large scale multi-agent systems. However, existing state-of-the-art algorithms do not fully exploit inter-agent coupling information to develop MARL algorithms. In this paper, we propose a systematic approach to leverage structures in the inter-agent couplings for efficient model-free reinforcement learning. We model the cooperative MARL problem via a Bayesian network and characterize the subset of agents, termed as the value dependency set, whose information is required by each agent to estimate its local action value function exactly. Moreover, we propose a partially decentralized training decentralized execution (P-DTDE) paradigm based on the value dependency set. We theoretically establish that the total variance of our P-DTDE policy gradient estimator is less than the centralized training decentralized execution (CTDE) policy gradient estimator. We derive a multi-agent policy gradient theorem based on the P-DTDE scheme and develop a scalable actor-critic algorithm. We demonstrate the efficiency and scalability of the proposed algorithm on multi-warehouse resource allocation and multi-zone temperature control examples. For dense value dependency sets, we propose an approximation scheme based on truncation of the Bayesian network and empirically show that it achieves a faster convergence than the exact value dependence set for applications with a large number of agents.
1 Introduction
The recent success of reinforcement learning (RL) has demonstrated strong evidence in achieving and exceeding human-level performance in complex tasks. RL has been studied extensively in a wide range of application domains such as games, robotics, logistics, transportation systems, and power systems. When multiple learning agents interact with the environment, the RL problem becomes much more challenging due to non-unique learning goals, non-stationarity, and the curse of dimensionality ([74]). The main hindrance to addressing the scalability issue in multi-agent reinforcement learning (MARL) is the combinatorial nature of MARL ([13, 74, 2]). Owing to the non-stationarity of the MARL problem, each agent needs to account for the joint-action value space which increases exponentially in the number of agents. In addition, for applications involving a large number of agents with limited communication such as networked systems, it is practically infeasible for an agent to have access to the global state, i.e., the states of all the agents. Instead, each agent has access to its local observations, which are a subset of the global state. This is referred to as partial observability111An alternative interpretation of partial observability has been used in the POMDP literature (see e.g., [56]), where each agent observes the state through noisy sensors. However, we do not use this interpretation in this paper. in MARL, which hinders the agents from learning the underlying policies of other agents, exacerbating the non-stationarity viewed by individual agents.
Scalability issues and partial observability have motivated the quest for finding more computationally efficient and scalable algorithms. Several information structures have been proposed to address these issues. The extreme case of partial observability is known as the independent learning (IL) scheme where each agent has access to only its own local action, observation and reward. This scheme suffers from non-convergence in general ([62]). To address the non-stationarity caused by the partial observability, it is common in the MARL literature to assume that the agents have access to a centralized entity that provides joint actions, joint observations and joint rewards during training and each agent uses its local observations during execution. This is referred to as the centralized training and decentralized execution (CTDE) scheme, which has been shown to be effective in a wide range of applications (see e.g., [33, 10, 47]). In the cooperative setting, the CTDE paradigm greatly simplifies the analysis, facilitating the use of tools developed in the analysis of single-agent setting ([74]). However, in many real-world scenarios the existence of a central entity either is infeasible or incurs a significant communication overhead due to a large amount of information exchange, degrading the scalability of the approach.
In this work, we focus on the scalability issue in the cooperative MARL setting, where the objective of the MARL problem is to find an optimal policy for each agent that maximizes the total accumulated rewards of all the agents. In many real-world applications, e.g., vehicle platooning, warehouse resource allocation, and multi-zone temperature control, structural information of the couplings between cooperative agents is known even in the model-free setting, either by design (e.g., placement of sensors) or by expert knowledge (e.g., the physical interactions/constraints among agents [73]). How to capture such information in designing efficient MARL algorithms is the main subject of this paper.
Contributions.This paper introduces a Multi-agent Bayesian Network (MABN) framework that enables exact Q-function decomposition for cooperative multi-agent reinforcement learning. Our key contributions are as follows:
-
•
We develop a systematic approach to leverage inter-agent couplings for exact Q-function decomposition without approximation errors. Unlike existing methods that approximate the value function through separable MDPs [19], approximate factorization [34], spatial exponential decay [43, 45], or monotonicity constraints [58, 47], our MABN framework identifies the minimal set of agents whose information is needed to compute the Q-function of each agent, thereby reducing the curse of dimensionality while maintaining optimality.
-
•
This paper extends our recent work [61] from the linear quadratic regulator (LQR) setting to a generic setting, where agents’ dynamics, rewards and observations can be nonlinear. Departing from the set-theoretic properties employed in [61], we develop the MABN, a unified probabilistic graphical model that explicitly encodes the individual inter-agent couplings in a single graph, allowing for efficient deduction of value dependency structures. The Bayesian network approach is more general and applicable to nonlinear systems with structural constraints.
-
•
The MABN framework applies to general partially observable stochastic cooperative games (POSCG) without restrictive assumptions on network topology, coupling density. It explicitly handles partial observability and remains effective even in densely coupled systems where convergence guarantees for approximation methods may not hold.
-
•
While the generality of MABN prevents from obtaining convergence guarantees or sample complexity bounds as in [61], we establish that our exact decomposition yields lower total variance compared to centralized training decentralized execution (CTDE) scheme suggesting improved sample complexity for gradient-based algorithms. We develop a cooperative policy gradient theorem for both stochastic and deterministic policies, which can be used to derive various RL algorithms.
-
•
We design a partially decentralized training and decentralized execution architecture (P-DTDE) based on the MABN structure for scalable learning. Further, we develop a multi-agent structured actor critic (MAStAC) algorithm based on the P-DTDE scheme which demonstrates superior performance across benchmark tasks.
Outline. The paper is outlined as follows. Section 2 introduces the problem formulation. Section 3 presents the main technical content, including the MABN approach for decomposition, our multi-agent policy gradient theorem, and the proposed MARL algorithm. Section 4 analyzes the variance in the gradient estimation and compares it with the CTDE scheme. Experimental results are presented in Section 5, followed by conclusions and limitations in Section 6.
2 Problem formulation
We consider a partially-observable stochastic cooperative game described by the tuple , where
- is the set of agent indices in the various graphs describing inter-agent couplings.
- is the edgeset of the state graph which defines the dynamics coupling between agents. An edge , if agent affects the state evolution of agent . Define the state index set of agent as .
- is the edgeset of the observation graph which defines the partial observability of agents. An edge , if agent is observed by agent . Define the observation index set of agent as . Partial observability in this paper means that each agent observes the states of its in-neighbors in , instead of all the agents.
- is the edgeset of the reward graph which defines the reward coupling among agents. An edge , if the state and action of agent affect the reward of agent . Define the reward index set of agent as .
- is the model that governs the state evolution of agent in time.
- are the state space and action space (discrete or continuous) of agent respectively.
- is the transition probability function of agent in the next time step, i.e., it specifies the probability distribution of agent transitioning to in the next time step when each agent takes action in the current state in the current time step.
- is defined as the partial observation of agent which includes the state information of all the agents that are observable by .
- is the instantaneous reward received by agent when each agent takes an action in the current state .
- is the discount factor that trades off the immediate and future rewards received by the agents.
To illustrate the three graphs , , and in a real-world setting, consider a multi-zone temperature control problem (with details in Appendix I). The temperature in each zone is affected by the heat conduction through walls from adjacent zones resulting in inherent physical couplings in the state transition dynamics. Thus, is an undirected graph with edges between zones that share a wall. If each zone can only measure its own temperature, the corresponding observation graph is fully decentralized. If the objective of the controller in each zone is to maintain the temperature in that zone, the resulting reward graph is also fully decentralized.
Let and denote the global state and global action of the multi-agent system (MAS) at time , respectively. Let and be the global policy function of the entire MAS and the local policy function of agent , respectively. To simplify exposition, we have chosen the policies ’s to be reactive. The formulation in this paper can be extended to allow history-based policies. We define the observation function for each agent , as a mapping such that .
At time of the game, agent observes the partial state , takes an action following its local policy function and receives a local reward . The accumulated discounted global reward is defined as
where is the global reward of the MAS at time . The global state value function and the global state-action value function for a given policy are defined as , and , respectively, where the expectation is taken over the state and state-action distributions, respectively. Define as agent ’s local state value function and as its local state-action value function.
The objective of the cooperative MARL problem is to find a globally optimal policy that maximizes the expected long term global reward when the MAS starts from a global state , takes a global action and follows a global policy , i.e.,
where is the distribution over the initial global state and global action. We parameterize the global policy by and denote it as . Similarly, the local policy of agent is denoted by . Given the global state , there exists a bijective mapping between the global policy and the collection of local policies . Specifically, the global policy can be decomposed as as the agents act independently based on their local observation . Conversely, the joint distribution over actions defined by the collection of local policies induces global policy when conditioned on the global state . The equivalence holds because in our formulation we assume that the global state contains all the information needed to determine each agent’s observation , and the joint action is uniquely determined by individual actions.. The optimization problem (2) can be rewritten in terms of as
We present a concrete example to illustrate the formulation. Consider a multi-agent system with time-invariant dynamics, where each agent’s dynamics and cost may depend on other agents’ states and actions and its observations are the states of a subset of all the agents. Specifically, the dynamics of agent is given by
where , , is the process noise, and the set contains the indices of the agents impacting the dynamics of agent . The reward (or negative cost) incurred by agent at time is given by
where the set contains the indices of the agents impacting the cost of agent . Agent observes the states of the agents in a set and prescribes a structured static feedback control
In this paper, we focus on solving the stochastic optimal control problem in a model-free setting, i.e.,
where are unknown but we can query given from a simulator.
3 Multi-agent Bayesian network and policy gradient theorem
Bayesian networks (BN) are an example of probabilistic graphical models (PGM) whose nodes represent random variables and edges encode the conditional dependence associated with them. RL and optimal control can be formulated using PGM to facilitate the use of probabilistic inference approaches This framework is referred to as the RL-as-inference framework that interprets finding optimal policies in RL as a form of probabilistic inference in graphical models. In this view, the agent’s objective to maximize rewards can be viewed as inferring the most probable actions or trajectories. Interested readers are referred to [28, 40, 41, 63]. The existing literature is largely focused on single agent decision making problems. We next propose a systematic approach to modeling the cooperative MARL problem in Section 2 as a BN, which we hope will enable future extensions of RL-as-inference paradigm to the cooperative MARL setting.
3.1 Construction of multi-agent Bayesian network
We define a BN which we refer to as the multi-agent Bayesian network (MABN) to represent the evolution of the MAS in a finite-horizon setting. The states and the actions of the agents are latent variables and a binary random variable for each agent is introduced as an observable variable. Assuming a negative reward structure, similar to the inference-based control approaches (see e.g., [64, 65, 49, 48, 17, 50, 72, 71, 60]), the log-likelihood that agent is optimal at time is proportional to , i.e.,
Given , the MABN is constructed as follows. 1) Assign a random variable to each , , and .
3.2 Deduction of action value function dependence from MABN
The formulation in Section 2 and the existing literature, e.g., [20], assume that the individual action value of agent , , depends on the global state and global action . However, during critic training in MARL, having computations dependent on the global state is undesirable as it incurs high communication cost and significantly hinders the scalability of the algorithms. Thus, a natural question arises: What is the minimal set of agents whose information is sufficient for an agent to estimate its action value function? We employ the MABN in Section 3.1 and derive a systematic approach to obtain such a minimal set. Consider the formulation in Section 2 with a finite horizon . We express , , as
To make the formulation more general and better reveal the dependency of on the inter-agent couplings, we assume that the sets , , and can be time-varying. Given the time-varying inter-agent couplings , , , for all , we define , and as the time-dependent reachability sets of agent ’s reward in the MABN at intermediate time (corresponding to ) and terminal time (corresponding to ), respectively. We further let
Theorem 3.2 below establishes that depends only on the state-action of agents in . Thus, we refer to as the value dependency set at time and the corresponding graph such that the in-neighbors of correspond to as the value dependency graph, denoted by .
Remark 3.1.
In general, a direct relationship between and cannot be established without the knowledge of , , and . However, under the formulation presented in this paper, the construction of MABN ensures that whenever . Futhermore, by the definition of in (10), we note that for all , accounts for one additional step of dependencies compared to . Hence, it follows that would generally be denser than . However, a precise relationship cannot be established without knowledge of the temporal evolution of and .
Theorem 3.2.
For any , depends only on .
The MABN constructed in Section 3.1 captures temporal evolution of the inter-agent couplings and provides an elegant way to compute in (10), . Owing to the causal relationship between variables in the MABN and the relationship between the local rewards and binary random variables in (8), can be obtained by tracing the predecessors of until time . We formalize this result as a property of the value dependency graph.
Property 3.3 (Value dependency graph).
At any time , an edge if and only if satisfying such that or can reach in the MABN.
Remark 3.4.
Note that for the formulation presented in this paper, if agent affects the value function of agent , then the state can definitely reach . In contrast, can reach if and only if agent influences agent indirectly through its state or reward neighbors. For example, observe that in Fig. 6, can reach which implies . But cannot reach because agent cannot affect agent through its state, or reward neighbors i.e., . However, the MABN generalizes to broader scenarios where only the actions of other agents are observable while the states remain hidden, or where the reward depends exclusively on the states of neighboring agents and are independent of their actions.
From Property 3.3, a brute-force solution to computing is to find the predecessors of , in the MABN using a path finding algorithm. However, the size of the PGM grows linearly in the horizon and exponentially in the number of agents. Therefore, performing path finding on the full MABN is computationally intensive. For time-invariant inter-agent couplings, the structure of the MABN repeats every two consecutive time steps (see e.g., Fig. 6 in Appendix B). We leverage this redundancy in the structure to make the path finding efficient for time-invariant inter-agent couplings. Specifically, we consolidate the MABN to two time steps by making edges between and , , (see e.g., Fig. 7 in Appendix B). We refer to this consolidated two time-step PGM as the folded MABN . The intuition for the bidirectional edge is that every traversal of the bidirectional edge in the folded graph implies evolution of a timestep in the full MABN. Because is time-invariant, Theorem 3.2 indicates that depends only on , i.e., .
Remark 3.5.
We remark that the MABN is capable of modeling the evolution of MAS in both finite and infinite horizon settings provided that the inter-agent couplings are known. However, since the identification of VD set employs path finding on the MABN (Property 3.3), a finite (finite-horizon setting) or receding (infinite-horizon setting) horizon is required for computational tractability.
3.3 Value gradient dependence and the multi-agent policy gradient theorem
The use of policy gradient theorems in our formulation requires computing the gradient of the global action value function with respect to individual action parameters , From Property 3.3, it follows that the action of agent affects the rewards of its out-neighbors in Thus, we define the gradient dependency graph and the corresponding index set , . We show in Theorem 3.6 that the gradient of with respect to can be decomposed as the sum of the gradients of , . Theorem 3.6 illustrates the interplay between the inter-agent couplings and the decomposition of the gradient of the objective function. The gradient dependency graph corresponds to the learning graph proposed in [20]. However, [20] neither considers the value dependency graph nor the MABN which are the two main contributions of this paper. We also establish that is the transpose graph of .
Theorem 3.6 (Gradient decomposition theorem).
If and are continuous and differentiable with respect to , , then
Proof.
Refer to Appendix D. ∎
We further let and define
Based on Theorem 3.2 and 3.6, Theorem 3.7 below presents the multi-agent policy gradient theorem for the formulation in Section 2.
Theorem 3.7.
Let be the policy for agent parameterized by and , where is defined in (11). Let be the expected global return.
-
(a)
if is deterministic, then .
-
(b)
if is stochastic, then .
The proof of Theorem 3.7 is deferred to Appendix E. This theorem establishes that the gradient of the global objective function in (3) with respect to , , can be obtained locally using the corresponding score function and the decomposed action-value function . In contrast, the existing multi-agent policy gradient theorem proposed in [75] requires estimation of the global value or advantage functions that depend on the global state, action, and reward. Therefore, the existing MARL algorithms usually require some form of centralized architecture or consensus during training to diffuse the local information of an agent over the network. Theorem 3.7 facilitates a sparse architecture for information exchange between a subset of agents, which we refer to as partially decentralized training and decentralized execution (P-DTDE) paradigm. Moreover, we theoretically establish variance reduction of the P-DTDE policy gradient estimator compared to the CTDE policy gradient estimators in Section 4 and empirically demonstrate the scalability and faster convergence of the P-DTDE paradigm in Section 5. The formulation presented in Section 2 is classified as Reactive Policy under Full Observability [3] i.e., the policy is reactive and the union of observations recovers the full state. Therefore, the state-based critics in our P-DTDE paradigm are unbiased [3, Theorem 4.3].
4 Variance analysis
In this section, we analyze the variance of the policy gradient estimator based on the proposed P-DTDE scheme. We obtain a general expression of the difference between the total variances of the CTDE and the P-DTDE stochastic policy gradient estimators, assuming that the gradient estimators produce biased estimates of the action-value function with non-zero variances. When both estimators achieve unbiased estimation, we characterize upper and lower bounds for the difference between the total variances and establish that the total variance of the P-DTDE policy gradient estimator is less than the CTDE policy gradient estimators.
Assumption 4.1.
The individual reward of each agent at any time is uniformly bounded i.e., , an such that
We consider two policy gradient estimators: one based on the CTDE paradigm (), where each agent estimates the global action value function based on the global state and action , and another based on the proposed P-DTDE paradigm (), where agent , , estimates given in (11), where we have let and . We further consider that the estimation of and is not exact. Let and be the errors in the estimation of and , respectively. The mean and variance of and are denoted as and , respectively. For stochastic policy gradient, the CTDE and P-DTDE estimators are given by
We obtain a general expression for the difference between the total variances of and in Appendix G. Our result shows that the difference in the total variances depends the variance of the policy gradient as well as the accuracy of the estimation in terms of and . Assuming that the estimation is unbiased, i.e., , Theorem 4.2 below establishes the upper and lower bounds on the difference between the total variances of the two estimators.
To state the theorem, we define the action value function after a subset of agents, say , take their actions as , and the corresponding advantage function of the subset of agents as , where
Theorem 4.2.
Define the score function . Then the total variances (tVar) of (the CTDE estimator) and (the P-DTDE estimator) with respect to the distributions of satisfy
where , , and
Proof.
Refer to Appendix G. ∎
The proof is deferred to Appendix G. A similar upper bound was obtained in [26] for a decentralized policy gradient estimator. However, in this paper, we analyze the lower bound of the total variance difference obtained by the decomposition of the Q-function. Given a fixed amount of samples, if , the centralized has the same number of parameters as the . Therefore, using the same estimation technique, it is expected that . However, when , the irrelevant agents contribute to additional noise in the estimation of the centralized , leading to a strictly higher variance, i.e., . Therefore, it is reasonable to assume that .
Observe that the error in the total variance of the CTDE and the proposed P-DTDE policy gradient estimators is proportional to the cardinality of with Thus, a sparser VD graph implies a higher cardinality of , leading to a larger lower bound in Theorem 4.2 implying a lower total variance of the P-DTDE estimator. If , and , the lower bound in (81) is non-zero and increases linearly with the second moment of . This signifies the effect of the proposed approach in achieving variance reduction compared to the centralized policy gradient estimator.
4.1 Multi-agent structured actor-critic algorithm
We now discuss the development of model-free multi-agent actor critic algorithms using Theorem 3.2, 3.6, and 3.7. In particular, we employ the deterministic policy gradient in Theorem 3.7(a) to develop an off-policy actor critic algorithm based on the P-DTDE paradigm, which we refer to as multi-agent structured actor critic (MAStAC) algorithm. The insights of value dependence in Section 3.2, 3.3 and Theorem 3.7 can be leveraged to design other policy gradient MARL algorithms, such as on-policy PPO algorithms.
In MAStAC, each agent is assigned an actor network , a critic network , and their corresponding target networks and . Training comprises two steps per agent. In the critic update step (Line 18), each agent updates its critic by minimizing the temporal difference (TD) error using a minibatch of size sampled from its replay buffer which stores tuples . Since is sufficient to compute , it suffices for each agent to maintain its local dependency information in . In the actor update step (Line 16), each actor minimizes its respective loss function. We employ a simultaneous implementation, where all the actor and critic networks are updated at the end of each epoch, while the target networks are updated at a slower timescale. Empirically, we observed that this simultaneous implementation outperforms coordinated implementation, where each actor, critic pair is updated sequentially. The schematic of the MAStAC is shown in Fig. 9 (Appendix F). The MAStAC algorithm is summarized in Algorithm 1.
Depending on the individual inter-gent couplings, the resulting might be dense. For example, consider , where , i.e., the edge set ‘transposed’ from . Although , , and are individually sparse, their combined effect yields a complete value dependency graph such that the decomposition in Theorem 3.2 requires the global state and action. To improve scalability, we propose a strategy to approximate the dense value dependency graph by a sparse graph. We approximate the value dependency set of an agent at time by considering only the predecessors of within time steps, yielding a sparser approximated value dependency graph . The intuition behind this approximation is that in the MABN, the states and actions have higher influence on the rewards in near future compared to those in the distant future. We formalize this approximation in Definition 4.3.
Definition 4.3 (approximated value dependency graph).
For time-invariant inter-agent couplings, , an edge , if and only if either or can reach within traversals of the bidirectional edge in .
Utilizing instead of in Algorithm 1 yields the ‘MAStAC approximated’ algorithm. When each agent’s reward and observation contain only its own state, the approximated value dependency graph recovers the hop neighbor approximation strategy [44, 45, 20]. Our interpretation of is based on temporal truncation of Q-value dependence which extends naturally to time-varying inter-agent couplings. In contrast, the approximation in [44, 45, 20] is in a spatial sense, i.e., it refers to the number of hops in the time-invariant inter-agent couplings.
5 Numerical experiments
We evaluate the MAStAC algorithm in Algorithm 1 using three numerical examples. In examples 1 and 2, each actor is initialized with 3 hidden layers of 64 neurons each with ReLu activation. A softmax function is applied at the output of each actor network to normalize the actions. In example 3, we use a linear actor network with tanh activation. In all the examples, each critic network is initialized with 3 hidden layers of 64 neurons each with ReLu activation. The actor and critic network parameters are initialized using xavier glorot initialization.
We compare Algorithm 1 with a variety of MARL baselines, including MADDPG ([33]), MATD3 ([1]), FACMAC ([42]). Though the deep coordination graph (DCG) proposed in [5] is not directly applicable, we implement a variant by decomposing a centralized critic in a similar fashion to the DCG in [5]. We refer to this baseline as ‘Q-deep coordination graph’ (QDCG). Specifically, we consider QDCG Decentralized and QDCG Centralized, which correspond the graph used to decompose the critic being a completely disconnected graph and a complete graph, respectively. The critic in QDCG Decentralized is equivalent to a centralized VDN critic architecture ([58]). Note that the DCG formulation (and QDCG) is applicable only to homogeneous observation spaces. Since the agents in Example 1 have heterogeneous observation spaces, we compare the QDCG’s performance to MAStAC in Example 2 and 3 only. To ensure a fair comparison, we omit COMA ([10]) as it applies to stochastic policies while MADDPG ([33]), MATD3 ([1]), FACMAC, and MAStAC all use deterministic policies. Similarly, COVDN and COMIX ([42]) are omitted as they do not have an actor network.
We run 15 Monte-Carlo (MC) simulations for each algorithm in each example. Figure 1 shows the comparison of the total average reward from the simulations across different benchmarks for the three examples. Table 1 summarizes the mean and standard deviation of the reward averaged over the final 20% of the epochs for the 15 simulations.
Example 1: warehouse resource allocation with a sparse . The -warehouse resource allocation problem was proposed in [78] and is described in Appendix H. We consider whose inter-agent couplings are shown in Fig. 10 (Appendix H). In the simulations, we set and , , where , if , and otherwise. We observe from Fig. 1 that the ‘MAStAC simultaneous and approximated’ achieve a faster convergence and have lesser variance compared to the other centralized baselines. This corroborates the effect of the decomposition described in Section 3. We also see from Table 1 that the ‘MAStAC simultaneous and approximated’ achieve the highest mean total average reward per episode and lower variance compared to the CTDE baselines. Although MADDPG, MATD3, and FACMAC may converge to comparable rewards to MAStAC (Simultaneous and Approximated),
they exhibit much slower rates of convergence compared to MAStAC. The faster rate of convergence of MAStAC highlights its strictly better sample efficiency compared to the CTDE baselines even for this small-scale example.
Ablation study To investigate the effect of and discussed in Theorem 3.2 and 3.6, we run the 9-warehouse example using two variants of the ‘MAStAC simultaneous’ algorithm: ‘MAStAC undecomposed Q’ where in (11), , i.e., without using the value dependency set, and ‘MAStAC undecomposed Qhat’ where the critic of agent directly learns a to replace in (11). Figure 2 shows the comparison of the total average reward for the three algorithms. After iterations the three algorithms achieved average rewards (MAStAC simultaneous), (MAStAC undecomposed Q), and (MAStAC undecomposed Qhat). At the iteration, the converged average rewards were , , and , respectively. These results demonstrate that both ‘MAStAC undecomposed Q’ and ‘MAStAC undecomposed Qhat’ exhibit slower convergence, validating the effectiveness of the value dependency set prescribed in Theorem 3.2. The performance gap underscores the computational and statistical benefits of leveraging inter-agent coupling information for exact Q-function decomposition.
Example 2: 40-warehouse resource allocation with a dense . To demonstrate the efficacy of the approximation strategy in Definition 4.3 and its scalability, we consider the warehouse resource allocation problem with , and individual local reward for each agent. This example yields a complete , i.e., , We observe from Fig. 1 that ‘MAStAC approximated’ with achieves the fastest convergence across the benchmarks. ‘MAStAC simultaneous’ has a relatively slower convergence compared to the approximated case due to the dense However, it converges to a higher reward compared to other centralized baselines, which demonstrates the scalability of the MAStAC approach. We note that QDCG centralized and QDCG decentralized perform better than MADDPG, MATD3, and FACMAC, owing to the coordination graph based decomposition. However, they converge to lower average rewards when compared to MAStAC (Simultaneous and Approximated), which signifies the effect of the exact decomposition of the Q-function discussed in Theorem 3.2. Fig. 11 (Appendix J) shows the comparison of ‘MAStAC approximated’ algorithm for different values of . As increases, the performance of the algorithm degrades due to the increased density of .
Example 3: structured multi-zone temperature control. We adapt the multi-zone temperature control problem ([31, 77, 30]) to a two-story building with zones on each level in a model-free setting as described in Appendix I. We prescribe and individual local reward for each agent. The resulting comprises 2 strongly connected components, each consisting of zones on one level. We observe from Fig. 1 that ‘MAStAC approximated’ with achieves the fastest convergence owing to the sparsity in From Table 1, we see that ‘MAStAC simultaneous’ converges to a higher reward than ‘MAStAC approximated’, which corroborates the exactness of the approximation in Theorem 3.2. Among the centralized baselines, we observe a similar trend as in Example 2 that MADDPG, MATD3, and FACMAC struggle to learn effectively whereas QDCG centralized and decentralized converge to a comparatively higher reward. However, MAStAC simultaneous and approximated algorithms converge to higher rewards and have lower variances compared to the QDCG centralized and decentralized algorithms.
Across the benchmark examples, the ‘MAStAC simultaneous and approximated’ algorithms exhibit reduced variance and faster convergence compared to the existing CTDE MARL algorithms, which demonstrates the advantage of the proposed decomposition approach. The performance gap between MAStAC and the CTDE algorithms increases considerably as the number of agents increase from Example 1 to Examples 2 and 3. Although the QDCG algorithms perform better than MADDPG, FACMAC, and MATD3, it is outperformed by the ‘MAStAC simultaneous and approximated’ algorithms in Example 2, 3. This can be attributed to the exact decomposition of the Q-function in Theorem 3.2 in contrast to the linear decomposition of the critic into individual and pairwise functions.
6 Conclusions
We employ a Bayesian network approach to incorporate structural information into MARL algorithms and achieve efficient learning via exact and approximate decomposition of the gradient of the global state-action value function. We establish conditions under which the stochastic policy gradient based on the exact decomposition exhibits lower variance than the centralized gradient, leading to improved sample complexity and scalability. Based on the decomposition, we introduce the P-DTDE scheme and develop the MAStAC algorithm. Through comparison with benchmark algorithms in numerical experiments, we demonstrate the enhanced convergence speed and reduced variance of the MAStAC. A limitation of the proposed approach is the availability of the inter-agent coupling structures. Future work will focus on removing this limitation by learning the structures as latent variables.
References
- [1] Johannes Ackermann, Volker Gabler, Takayuki Osa, and Masashi Sugiyama. Reducing overestimation bias in multi-agent domains using double centralized critics. arXiv preprint arXiv:1910.01465, 2019.
- [2] Stefano V. Albrecht, Filippos Christianos, and Lukas Schäfer. Multi-Agent Reinforcement Learning: Foundations and Modern Approaches. MIT Press, 2024.
- [3] Andrea Baisero and Christopher Amato. Unbiased asymmetric reinforcement learning under partial observability. arXiv preprint arXiv:2105.11674, 2021.
- [4] Matteo Bettini, Ajay Shankar, and Amanda Prorok. Heterogeneous multi-robot reinforcement learning. arXiv preprint arXiv:2301.07137, 2023.
- [5] Wendelin Böhmer, Vitaly Kurin, and Shimon Whiteson. Deep coordination graphs. In International Conference on Machine Learning, pages 980–991. PMLR, 2020.
- [6] Maxime Bouton, Jaeseong Jeong, Jose Outes, Adriano Mendo, and Alexandros Nikou. Multi-agent reinforcement learning with graph Q-networks for antenna tuning. In NOMS 2023-2023 IEEE/IFIP Network Operations and Management Symposium, pages 1–7. IEEE, 2023.
- [7] Filippos Christianos, Georgios Papoudakis, and Stefano V Albrecht. Pareto actor-critic for equilibrium selection in multi-agent reinforcement learning. Transactions on Machine Learning Research, 2023.
- [8] Xiangxiang Chu and Hangjun Ye. Parameter sharing deep deterministic policy gradient for cooperative multi-agent reinforcement learning. arXiv preprint arXiv:1710.00336, 2017.
- [9] Shifei Ding, Wei Du, Ling Ding, Jian Zhang, Lili Guo, and Bo An. Multiagent reinforcement learning with graphical mutual information maximization. IEEE Transactions on Neural Networks and Learning Systems, 2023.
- [10] Jakob Foerster, Gregory Farquhar, Triantafyllos Afouras, Nantas Nardelli, and Shimon Whiteson. Counterfactual multi-agent policy gradients. Proceedings of the AAAI Conference on Artificial Intelligence, 32(1), Apr. 2018.
- [11] Carlos Guestrin, Daphne Koller, and Ronald Parr. Multiagent planning with factored MDPs. Advances in neural information processing systems, 14, 2001.
- [12] Carlos Guestrin, Michail Lagoudakis, and Ronald Parr. Coordinated reinforcement learning. In ICML, volume 2, pages 227–234. Citeseer, 2002.
- [13] Pablo Hernandez-Leal, Bilal Kartal, and Matthew E. Taylor. A survey and critique of multiagent deep reinforcement learning. Autonomous Agents and Multi-Agent Systems, 33(6):750–797, October 2019.
- [14] Yifan Hu, Junjie Fu, and Guanghui Wen. Graph soft actor–critic reinforcement learning for large-scale distributed multirobot coordination. IEEE transactions on neural networks and learning systems, 2023.
- [15] Aamal Hussain, Dan Leonte, Francesco Belardinelli, Raphael Huser, and Dario Paccagnan. Multi-agent q-learning dynamics in random networks: Convergence due to exploration and sparsity. arXiv preprint arXiv:2503.10186, 2025.
- [16] Shariq Iqbal and Fei Sha. Actor-attention-critic for multi-agent reinforcement learning. In Proceedings of the 36th International Conference on Machine Learning, volume 97 of Proceedings of Machine Learning Research, pages 2961–2970. PMLR, 09–15 Jun 2019.
- [17] Hideaki Itoh, Yoshitaka Sakai, Toru Kadoya, Hisao Fukumoto, Hiroshi Wakuya, and Tatsuya Furukawa. Using model uncertainty for robust optimization in approximate inference control. Artificial Life and Robotics, 22(3):327–335, 2017.
- [18] Jiechuan Jiang, Chen Dun, Tiejun Huang, and Zongqing Lu. Graph convolutional reinforcement learning. arXiv preprint arXiv:1810.09202, 2018.
- [19] Ruiyang Jin, Zaiwei Chen, Yiheng Lin, Jie Song, and Adam Wierman. Approximate global convergence of independent learning in multi-agent systems. arXiv preprint arXiv:2405.19811, 2024.
- [20] Gangshan Jing, He Bai, Jemin George, Aranya Chakrabortty, and Piyush K Sharma. Distributed multi-agent reinforcement learning based on graph-induced local value functions. IEEE Transactions on Automatic Control, 2024.
- [21] Hsu Kao and Vijay Subramanian. Common information based approximate state representations in multi-agent reinforcement learning. In Proceedings of The 25th International Conference on Artificial Intelligence and Statistics, volume 151 of Proceedings of Machine Learning Research, pages 6947–6967. PMLR, 28–30 Mar 2022.
- [22] Soummya Kar, José M. F. Moura, and H. Vincent Poor. -learning: A collaborative distributed strategy for multi-agent reinforcement learning through . IEEE Transactions on Signal Processing, 61(7):1848–1862, 2013.
- [23] Nouman Khan and Vijay Subramanian. Cooperative multi-agent constrained pomdps: Strong duality and primal-dual reinforcement learning with approximate information states. arXiv preprint arXiv:2307.16536, 2023.
- [24] Jelle R. Kok and Nikos Vlassis. Using the max-plus algorithm for multiagent decision making in coordination graphs, page 1–12. Springer-Verlag, Berlin, Heidelberg, 2006.
- [25] Daphne Koller and Ronald Parr. Computing factored value functions for policies in structured MDPs. In IJCAI, volume 99, pages 1332–1339, 1999.
- [26] Jakub Grudzien Kuba, Muning Wen, Yaodong Yang, Linghui Meng, Shangding Gu, Haifeng Zhang, David Henry Mguni, and Jun Wang. Settling the variance of multi-agent policy gradients. arXiv preprint arXiv:2108.08612, 2021.
- [27] Yuheng Lei, Jianyu Chen, Shengbo Eben Li, and Sifa Zheng. Zeroth-order actor-critic. arXiv preprint arXiv:2201.12518, 2022.
- [28] Sergey Levine. Reinforcement learning and control as probabilistic inference: Tutorial and review. arXiv preprint arXiv:1805.00909, 2018.
- [29] Wenhao Li, Bo Jin, Xiangfeng Wang, Junchi Yan, and Hongyuan Zha. F2A2: Flexible fully-decentralized approximate actor-critic for cooperative multi-agent reinforcement learning. Journal of Machine Learning Research, 24(178):1–75, 2023.
- [30] Yingying Li, Yujie Tang, Runyu Zhang, and Na Li. Distributed reinforcement learning for decentralized linear quadratic control: A derivative-free policy optimization approach. IEEE Transactions on Automatic Control, 67(12):6429–6444, 2022.
- [31] Yashen Lin, Timothy Middelkoop, and Prabir Barooah. Issues in identification of control-oriented thermal models of zones in multi-zone buildings. In 2012 IEEE 51st IEEE Conference on Decision and Control (CDC), pages 6932–6937, 2012.
- [32] Xiangyu Liu and Kaiqing Zhang. Partially observable multi-agent reinforcement learning with information sharing. arXiv preprint arXiv:2308.08705, 2023.
- [33] Ryan Lowe, Yi I Wu, Aviv Tamar, Jean Harb, OpenAI Pieter Abbeel, and Igor Mordatch. Multi-agent actor-critic for mixed cooperative-competitive environments. Advances in neural information processing systems, 30, 2017.
- [34] Chenbei Lu, Laixi Shi, Zaiwei Chen, Chenye Wu, and Adam Wierman. Overcoming the curse of dimensionality in reinforcement learning through approximate factorization. arXiv preprint arXiv:2411.07591, 2024.
- [35] Yanhao Ma and Jie Luo. Value-decomposition multi-agent proximal policy optimization. In 2022 China Automation Congress (CAC), pages 3460–3464, 2022.
- [36] Sergio Valcarcel Macua, Aleksi Tukiainen, Daniel García-Ocaña Hernández, David Baldazo, Enrique Munoz de Cote, and Santiago Zazo. Diff-DAC: Distributed actor-critic for average multitask deep reinforcement learning. In Adaptive Learning Agents (ALA) Conference, 2018.
- [37] Hangyu Mao, Zhengchao Zhang, Zhen Xiao, and Zhibo Gong. Modelling the dynamic joint policy of teammates with attention multi-agent ddpg. In Proceedings of the 18th International Conference on Autonomous Agents and MultiAgent Systems, AAMAS ’19, page 1108–1116, Richland, SC, 2019. International Foundation for Autonomous Agents and Multiagent Systems.
- [38] Navid Naderializadeh, Fan H Hung, Sean Soleyman, and Deepak Khosla. Graph convolutional value decomposition in multi-agent reinforcement learning. arXiv preprint arXiv:2010.04740, 2020.
- [39] Siddharth Nayak, Kenneth Choi, Wenqi Ding, Sydney Dolan, Karthik Gopalakrishnan, and Hamsa Balakrishnan. Scalable multi-agent reinforcement learning through intelligent information aggregation. In International Conference on Machine Learning, pages 25817–25833. PMLR, 2023.
- [40] Erfaun Noorani and John S. Baras. A probabilistic perspective on risk-sensitive reinforcement learning. In 2022 American Control Conference (ACC), pages 2697–2702, 2022.
- [41] Brendan O’Donoghue, Ian Osband, and Catalin Ionescu. Making sense of reinforcement learning and probabilistic inference. In International Conference on Learning Representations, 2020.
- [42] Bei Peng, Tabish Rashid, Christian Schroeder de Witt, Pierre-Alexandre Kamienny, Philip Torr, Wendelin Böhmer, and Shimon Whiteson. FACMAC: Factored multi-agent centralised policy gradients. Advances in Neural Information Processing Systems, 34:12208–12221, 2021.
- [43] Chao Qu, Shie Mannor, Huan Xu, Yuan Qi, Le Song, and Junwu Xiong. Value propagation for decentralized networked deep multi-agent reinforcement learning. Advances in Neural Information Processing Systems, 32, 2019.
- [44] Guannan Qu and Na Li. Exploiting fast decaying and locality in multi-agent mdp with tree dependence structure. In 2019 IEEE 58th Conference on Decision and Control (CDC), page 6479–6486. IEEE Press, 2019.
- [45] Guannan Qu, Adam Wierman, and Na Li. Scalable reinforcement learning for multiagent networked systems. Oper. Res., 70(6):3601–3628, nov 2022.
- [46] Tabish Rashid, Gregory Farquhar, Bei Peng, and Shimon Whiteson. Weighted qmix: Expanding monotonic value function factorisation for deep multi-agent reinforcement learning. Advances in neural information processing systems, 33:10199–10210, 2020.
- [47] Tabish Rashid, Mikayel Samvelyan, Christian Schroeder de Witt, Gregory Farquhar, Jakob Foerster, and Shimon Whiteson. QMIX: Monotonic value function factorisation for deep multi-agent reinforcement learning, 2018.
- [48] Konrad Rawlik, Marc Toussaint, and Sethu Vijayakumar. An approximate inference approach to temporal optimization in optimal control. Advances in neural information processing systems, 23, 2010.
- [49] Elmar A Rückert and Gerhard Neumann. Stochastic optimal control methods for investigating the power of morphological computation. Artificial Life, 19(1):115–131, 2013.
- [50] Elmar Rueckert, Max Mindt, Jan Peters, and Gerhard Neumann. Robust policy updates for stochastic optimal control. In 2014 IEEE-RAS International Conference on Humanoid Robots, pages 388–393. IEEE, 2014.
- [51] Heechang Ryu, Hayong Shin, and Jinkyoo Park. Multi-agent actor-critic with generative cooperative policy network. arXiv preprint arXiv:1810.09206, 2018.
- [52] Esmail Seraj, Zheyuan Wang, Rohan Paleja, Anirudh Patel, and Matthew Gombolay. Learning efficient diverse communication for cooperative heterogeneous teaming. Technical report, Sandia National Lab.(SNL-NM), Albuquerque, NM (United States), 2022.
- [53] Siqi Shen, Yongquan Fu, Huayou Su, Hengyue Pan, Peng Qiao, Yong Dou, and Cheng Wang. Graphcomm: A graph neural network based method for multi-agent reinforcement learning. In ICASSP 2021-2021 IEEE International Conference on Acoustics, Speech and Signal Processing (ICASSP), pages 3510–3514. IEEE, 2021.
- [54] David Silver, Guy Lever, Nicolas Heess, Thomas Degris, Daan Wierstra, and Martin Riedmiller. Deterministic policy gradient algorithms. In Eric P. Xing and Tony Jebara, editors, Proceedings of the 31st International Conference on Machine Learning, volume 32 of Proceedings of Machine Learning Research, pages 387–395, Bejing, China, 22–24 Jun 2014. PMLR.
- [55] Kyunghwan Son, Daewoo Kim, Wan Ju Kang, David Earl Hostallero, and Yung Yi. QTRAN: Learning to factorize with transformation for cooperative multi-agent reinforcement learning. In Kamalika Chaudhuri and Ruslan Salakhutdinov, editors, Proceedings of the 36th International Conference on Machine Learning, volume 97 of Proceedings of Machine Learning Research, pages 5887–5896. PMLR, 09–15 Jun 2019.
- [56] Matthijs TJ Spaan. Partially observable markov decision processes. In Reinforcement learning: State-of-the-art, pages 387–414. Springer, 2012.
- [57] Jianyu Su, Stephen Adams, and Peter Beling. Value-decomposition multi-agent actor-critics. In Proceedings of the AAAI conference on artificial intelligence, volume 35, pages 11352–11360, 2021.
- [58] Peter Sunehag, Guy Lever, Audrunas Gruslys, Wojciech Marian Czarnecki, Vinicius Zambaldi, Max Jaderberg, Marc Lanctot, Nicolas Sonnerat, Joel Z Leibo, Karl Tuyls, et al. Value-decomposition networks for cooperative multi-agent learning. arXiv preprint arXiv:1706.05296, 2017.
- [59] Richard S Sutton and Andrew G Barto. Reinforcement learning: An introduction. MIT press, 2018.
- [60] Shahbaz P Qadri Syed and He Bai. Approximate constrained stochastic optimal control via parameterized input inference. Automatica, 171:111978, 2025.
- [61] Shahbaz P Qadri Syed and He Bai. Exploiting inter-agent coupling information for efficient reinforcement learning of cooperative LQR. In 7th Annual Learning for Dynamics & Control Conference, pages 1378–1391. PMLR, 2025.
- [62] Ming Tan. Multi-agent reinforcement learning: Independent vs. cooperative agents. In Proceedings of the tenth international conference on machine learning, pages 330–337, 1993.
- [63] Jean Tarbouriech, Tor Lattimore, and Brendan O’Donoghue. Probabilistic inference in reinforcement learning done right. In Sixteenth European Workshop on Reinforcement Learning, 2023.
- [64] Emanuel Todorov. General duality between optimal control and estimation. In 47th IEEE Conference on Decision and Control, pages 4286–4292. IEEE, 2008.
- [65] Marc Toussaint. Robot trajectory optimization using approximate inference. In Proceedings of the 26th annual international conference on machine learning, pages 1049–1056, 2009.
- [66] Petar Veličković, Guillem Cucurull, Arantxa Casanova, Adriana Romero, Pietro Lio, and Yoshua Bengio. Graph attention networks. arXiv preprint arXiv:1710.10903, 2017.
- [67] Haozhi Wang, Yinchuan Li, Qing Wang, Yunfeng Shao, and Jianye Hao. Multi-agent policy reciprocity with theoretical guarantee. arXiv preprint arXiv:2304.05632, 2023.
- [68] Jianhao Wang, Zhizhou Ren, Beining Han, Jianing Ye, and Chongjie Zhang. Towards understanding cooperative multi-agent q-learning with value factorization. Advances in Neural Information Processing Systems, 34:29142–29155, 2021.
- [69] Rose E. Wang, Michael Everett, and Jonathan P. How. R-maddpg for partially observable environments and limited communication. arXiv preprint, arXiv:2002.06684, 2020.
- [70] Yihan Wang, Beining Han, Tonghan Wang, Heng Dong, and Chongjie Zhang. DOP: Off-policy multi-agent decomposed policy gradients. In International conference on learning representations, 2020.
- [71] Joe Watson, Hany Abdulsamad, Rolf Findeisen, and Jan Peters. Efficient stochastic optimal control through approximate Bayesian input inference. arXiv preprint, arXiv:2105.07693, 2021.
- [72] Joe Watson, Hany Abdulsamad, and Jan Peters. Stochastic optimal control as approximate input inference. In Proceedings of Conference on Robot Learning, pages 697–716. PMLR, 2020.
- [73] Ziyao Yang, Amol D Gaidhane, Ján Drgoňa, Vikas Chandan, Mahantesh M Halappanavar, Frank Liu, and Yu Cao. Physics-constrained graph modeling for building thermal dynamics. Energy and AI, 16:100346, 2024.
- [74] Kaiqing Zhang, Zhuoran Yang, and Tamer Başar. Multi-Agent Reinforcement Learning: A Selective Overview of Theories and Algorithms, pages 321–384. Springer International Publishing, Cham, 2021.
- [75] Kaiqing Zhang, Zhuoran Yang, Han Liu, Tong Zhang, and Tamer Basar. Fully decentralized multi-agent reinforcement learning with networked agents. In International conference on machine learning, pages 5872–5881. PMLR, 2018.
- [76] Kaiqing Zhang, Zhuoran Yang, Han Liu, Tong Zhang, and Tamer Başar. Finite-sample analysis for decentralized batch multiagent reinforcement learning with networked agents. IEEE Transactions on Automatic Control, 66(12):5925–5940, 2021.
- [77] Xuan Zhang, Wenbo Shi, Xiwang Li, Bin Yan, Ali Malkawi, and Na Li. Decentralized temperature control via hvac systems in energy efficient buildings: An approximate solution procedure. In 2016 IEEE Global Conference on Signal and Information Processing (GlobalSIP), pages 936–940, 2016.
- [78] Yan Zhang and Michael M Zavlanos. Cooperative multi-agent reinforcement learning with partial observations. IEEE Transactions on Automatic Control, 2023. doi: 10.1109/TAC.2023.3288025.
Supplementary Material
Appendix A Related work
CTDE in MARL Our proposed approach sparsifies the centralized structure during training in the CTDE paradigm thereby improving the rate of convergence. The MADDPG algorithm [33] employs CTDE paradigm where each agent trains a DDPG algorithm such that the actor uses only the local observations whereas the critic for each agent has access to the global observation and global policy during training. The MADDPG algorithm was demonstrated in various cooperative, competitive and mixed settings. However, as each critic accumulates the information of all of the agents it encounters the curse of dimensionality and thus does not scale to larger number of agents. Several variants of the MADDPG algorithm have been proposed to tackle different problems. For example, see [8, 16, 37, 51, 69]. Concurrently, [10] proposed the counterfactual multi-agent policy gradient algorithm (COMA) to address the credit assignment problem in cooperative multi-agent settings utilizing a centralized critic and decentralized actors. COMA algorithm uses a counterfactual baseline to marginalize out the action of a particular agent and compares the estimated return to that of the joint action. The key difference between MADDPG and COMA algorithms is that the COMA algorithm trains a single critic shared by all the agents whereas the MADDPG algorithm trains a separate centralized critic for each agent making it applicable to competitive environments with continuous action spaces. In addition, the MADDPG algorithm is not designed to handle the multi-agent credit assignment problem. The centralized value functions mitigate several challenges in MARL such as non-stationarity, partial observability, multi-agent credit assignment, equilibrium selection [7]. However, the exponential growth of the joint action space with the number of agents poses several computational and learning challenges: (1) difficulty in learning the centralized value function due to curse of dimensionality, (2) inefficient decentralized action selection requiring coordination among exponentially large number of joint actions, (3) high computational overhead in evaluating all possible action combinations for greedy action selection [2].
Value function factorization An alternative class of algorithms in the MARL literature are those based on value function factorization (see e.g., [25, 11, 58, 47, 55]) that approximate centralized value functions with efficient training and decentralized execution. The main idea in value function factorization is to decompose the centralized action value function as a combination of individual action value functions (utilities) that can be efficiently learned and executed in a decentralized fashion. However, to ensure that the decentralized action selection with respect to individual utilities leads to effective joint actions, the decomposition should satisfy the Individual global max (IGM) property [47, 55]. The IGM property states that the greedy joint action with respect to the centralize action value function is equal to the composition of the individual greedy actions of the agents that maximize the individual utility functions. Therefore the IGM property facilitates the efficient computation of the greedy joint action during training by computing the greedy individual actions with respect to the individual utility functions. Moreover, as the aggregation of the individual action value functions is jointly optimized, the contribution of a particular agent’s action to the common reward is distinguishable which addresses the multi-agent credit assignment problem.
Two representative examples of value function factorization are VDN [58] and QMIX [47] algorithms. VDN proposes a linear decomposition of the centralized action value function which satisfies the IGM property [2, Section 9.5.2]. In VDN, each agent consists of an actor network and a critic network that depend on its local information. During training, the centralized action value in the loss is computed as the summation of the individual action values of all the agents allowing the agents to incorporate the impact of other agents’ actions. Although the linear decomposition used in VDN is simple, in many real world scenarios the decomposition of the centralized action value function is better represented as a nonlinear function of the individual action value functions. Hence, the QMIX algorithm uses a similar architecture as the VDN except that it uses an additional mixer network that takes the individual actions value functions as input and outputs the decomposed centralized action value function. The mixer network is enforced with positive weights to ensure that the centralized action-value function is monotonic with respect to the individual utilities which is a sufficient condition to satisfy the IGM property [2, Section 9.5.3]. However, the structural constraints such as additivity and monotonicity are too restrictive to factorize the centralized value function in some environments. Therefore, [55] proposed the QTRAN algorithm that formulates a slightly less restrictive decomposition which still satisfies the IGM property [55]. This is done by learning an additional centralized value function that corrects the discrepancy between the centralized and decomposed action value functions caused due to the partial observability of agents. Several other variants of the value decomposition methods such as FACMAC [42], DOP [70], VDA2C [57], VDPPO [35] have been proposed in the literature. Although the value factorization techniques perform well in many scenarios nonetheless they require a centralized scheme such as linear or nonlinear combination of the individual action value functions. In contrast, in the proposed P-DTDE scheme, each critic requires only the states and actions of agents in its value dependency set during training. In addition, we pursue the exact decomposition in the P-DTDE scheme given the coupling information while the value factorization techniques focus on learning approximated decompositions.
Coordination graphs (CG) [11, 12, 24] are another advanced value factorization technique for managing the complexity of joint action spaces. These graphs represent agents as nodes and specify coordination dependencies between them as edges, facilitating the decomposition of the centralized action value function as the sum of individual value functions that depend only on interacting subsets of agents. CGs are an example of the coupled reward setting. Similarly, the coupled dynamics and partial observability are two other types of inter-agent couplings studied in the MARL literature. However, in the most general setting the optimal action of an agent should take into account the combined effect of the aforementioned couplings. Hence, [20] proposed a distributed RL algorithm based on zeroth order optimization that incorporates all the three types of inter-agent couplings in the cooperative MARL setting. In particular, [20] deduce a new graph called the learning graph that represents the information flow during learning. This work has a strong relevance to the approach present in this paper. Therefore, we highlight the key distinctions and contributions of the proposed approach over those presented in [20]. First, [20] use a set-theoretic approach and treat the couplings as distinct graphs whereas in this work all the inter-agent coupling information is aggregated into a single Bayesian network. Second, the individual value functions require the information of the global state whereas in this work we characterize a subset of agents that affect the individual action value function resulting in the P-DTDE architecture. Third, [20] uses zero-order optimization and a REINFORCE algorithm which have higher sample complexity and variance in parameter updates compared to the actor-critic methods proposed in this work [27].
Graph neural networks (GNNs) have gained attention recently in the field of MAS due to their ability to effectively tackle graph-structured applications. For example, DGN [18] uses the graph attention network (GAT) [66] to aggregate the information of neighboring agents for each agent and passes the aggregated information as the input to the action value function. GraphComm [53] categorizes relationships among agents into explicit and implicit categories. The exchange of information about both static and dynamic relationships among agents is facilitated through GAT. InforMARL [39] treats all entities in the environment, including target points, and obstacles as nodes in the graph, and uses a GNN to aggregate information of the local neighborhoods of the agents and input it into the actor and critic. GNNs have also been used to learn effective communication models for cooperative agents (e.g., [52]). These GNN-based methods require additional communications for decentralized execution and may not satisfy observation constraints in the MAS. HetGPPO [4] employs a GNN to aggregate information from neighbors to empirically learn local policies and critics. Recent work [38, 9, 6, 14] employs GNNs (with a given graph) to learn state-action value factorization via QMIX or VDN, which are approximations.
Approximate information states is another parallel line of research in MARL that has emerged to address partial observability and information asymmetry in multi-agent learning. [21] develops a general compression framework with approximate private state representations to construct decentralized policies. [23] develops a primal-dual MARL framework with approximate information states for cooperative multi-agent constrained POMDPs. The approximate information states are characterized independently of Lagrange multipliers, enabling adaptation during learning without requiring new representations. [32] demonstrates that information sharing among agents can achieve quasi-efficient statistical and computational guarantees in partially observable stochastic games. These information-theoretic frameworks address a complementary challenge to the structural value decomposition discussed in this paper. We leverage the known inter-agent couplings to identify which agents should share information of Q-function evaluation (exact decomposition) whereas the approximate information state-based approaches focus on what information should be shared among agents to enable efficient coordination.
In distributed RL [22, 75, 36, 78, 76, 29, 43], the most common method is to use a consensus algorithm for each agent to estimate the global reward function with only local information from neighbors. The performance of such distributed RL algorithms is worse than centralized RL algorithms, as it takes time to reach consensus and the learning is conducted with the estimated global reward. However, our approach pursues exact decomposition of the policy gradient to facilitate efficient learning in a partially decentralized fashion. When such decomposition involves only a subset of the agents or appropriate approximation methods are designed, our approach is expected to be more efficient than the centralized RL.
There is a recent line of work that derives convergence guarantees for MARL under specific assumptions such as separability [19], approximate factorization [34], network sparsity/connectivity [67, 15], IGM property [68, 46], and finite/discrete state-,action-spaces [19, 68]. These approaches rely on approximating the underlying MDP or imposing network constraints, which can result in loss of guarantee in densely coupled MDPs or arbitrary network interaction systems. In contrast, our proposed MABN framework is general: it yields the exact value function decomposition applicable for arbitrarily dense inter-agent couplings and explicitly handles partial observability, remaining effective even in settings where the assumptions for convergence results do not hold. In such a general framework, obtaining provable convergence guarantees is challenging and a problem of independent interest. As we show (in Section G) that the total variance of policy gradient estimation is lower with the decomposition, we expect that the gradient descent type algorithms will show faster convergence and better sample complexity.
Appendix B Illustration of MABN
Appendix C Proof of Theorem 3.2
Proof.
Consider the finite-horizon case of the partially-observable stochastic cooperative game described in Section 2 with time-varying inter-agent couplings. For any agent at time , the local reward is deterministic given The state transition of agent at time depends only on and the action of agent depends only on Therefore, the innermost expectation in (9) is fully defined over where Similarly, the expectation at time is taken over where Therefore, at any time the corresponding expectation in (9) is taken over where Then, (9) can be rewritten as
From (13), it is evident that the individual action value of agent at time , , depends only on which is governed by the time-varying inter-agent couplings. ∎
Appendix D Proof of Theorem 3.6
Proof.
Recall that
The first term in (14) can be rewritten as
Assuming a deterministic global reward given global state and global action yields
Similarly, the second term in (14) can be rewritten as
Using (16) and (17), (14) is rewritten as
Using the linearity of expectation, (18) can be rewritten as
where . From Theorem 3.2, the reward of each agent depends on , and by the definition of . Therefore, if , then . Hence, is independent of and thus . It then follows that , are independent of , , which implies
where the second line in (22) is obtained by interchanging the derivative and integral assuming that each is sufficiently smooth in . Then the gradient of the global action value function with respect to is given by
∎
Appendix E Proof of Theorem 3.7
Proof.
-
(a)
We follow a similar approach as the single agent deterministic policy gradient theorem in [54] except that we leverage the topology of the network defining the multi-agent couplings. The regularity conditions required for the proof are stated as and when needed. To compute the policy gradient for an agent , consider the gradient of the state value function with respect to :
Using Theorem 3.6, the gradient of GVF w.r.t can be rewritten
In the remainder of the proof, we suppress the notation for brevity. Assuming , and their derivatives are sufficiently smooth in , interchanging the gradient and integral yields
Let denote the visitation probability of transitioning from to in steps under policy . The transition from state to in steps can be broken down into transition from to an intermediate state in steps and transition from to in the last step. Hence, the visitation probability can be expressed recursively as
For brevity, define . Using (29), the gradient in (28) is expressed in a recursive form as
For the sake of analysis, we assume that the state space is compact. Hence, we assume that is finite and apply Fubini’s theorem to exchange the order of integration which yields
Hence, the gradient of the objective can be rewritten as
Assuming , and their derivatives with respect to are sufficiently smooth, applying Leibniz rule to exchange the derivative and integral and applying Fubini’s theorem to exchange the order of integration assuming is finite yields
Denote
-
(b)
We follow a similar approach as the single agent stochastic policy gradient theorem in [59] except that we leverage the topology of the network defining the multi-agent couplings. The regularity conditions required for the proof are stated as and when needed. To compute the policy gradient for an agent , consider the gradient of the state value function with respect to :
Assume that , are sufficiently smooth and the Then, interchanging the derivative and integral and using Theorem 3.6 yields
Recall that
Assuming a deterministic reward and a sufficiently smooth and its derivative with respect to yields
Let denote the visitation probability of transitioning from to in steps under policy . The transition from state to in steps can be broken down into transition from to an intermediate state in steps and transition from to in the last step. Hence, the visitation probability can be expressed recursively as
Define . For the sake of analysis, we assume that the state space is compact which implies is finite. Therefore, in the remainder of the proof, we apply Fubini’s theorem to exchange the order of integration when necessary. Then, using (48), the gradient in (47) is expressed in a recursive form as
Hence, the gradient of the objective can be rewritten as
Denote
The last two equations follow from the fact that is a constant (equal to the length of episode in episodic case and 1 in continuous case) and is a stationary distribution. Define . From the definition of set, only depends on such that . Hence, (57) can be rewritten as
Replacing by and by gives the result in Theorem 3.7 b.
∎
Appendix F Schematic of the structured actor critic (MAStAC) algorithm
Appendix G Proof of Theorem 4.2
Proof.
Let
be the centralized and the decomposed Q-estimators over the distribution , respectively. Let Then,
Using the law of total variance, we express the difference in the variance of the and as
Assuming that is compact and are finite, we employ Fubini’s theorem to exchange the order of integration as and when needed. Consider the first and fourth terms of (59):
We note that
Similarly,
Consider
Similarly, it is straightforward to show that
Then, using (63) and (64), the second term and the fifth term in (59) can be expressed as
For the third term in (59), we note
Similarly, for the sixth term in (59), it follows that
Using (66) and (67), we obtain
Substituting (61), (62), (65), and (68) into (59) yields
If , then
We note that
Assuming that is sufficiently smooth, we interchange the derivative and integral to obtain
Therefore,
which means
Define such that , and , Since (due to Theorem 3.2), (73) can be rewritten as
Upper bounding in the first term by yields
Assumption 4.1 implies that the advantage function is bounded. Therefore, by the completeness axiom, exists. Thus,
To characterize the lower bound we note that from the definition of multi-agent advantage function and (21), and is independent of from Theorem 3.6. Hence, we rewrite the first term in (74) as
Let . Then
The upper bound implies that if , the variance of decreases linearly with the increase of the number of agents in . A similar upper bound was obtained in [26] for a decentralized policy gradient estimator. However, to the best of our knowledge this is the first attempt to analyze the lower bound of the total variance difference. If and then the lower bound in (81) is non-zero which means that is strictly less than , and the lower bound increases as the variance of increases. For example, when all the agents take exploratory actions, then the variance of is greater than the variance of owing to the decomposition in Theorem 3.2, 3.6. This signifies the effect of the proposed approach in achieving variance reduction compared to the centralized policy gradient estimator. ∎
Appendix H Warehouse resource allocation problem
Consider a group of -warehouses that can consume, observe and transfer resources among each other based on a set of pre-determined graphs. Given a state graph , an observation graph , and a reward graph , the goal of the resource allocation problem is to find an optimal allocation strategy that guarantees adequate resources for each warehouse to meet its local demand. Each warehouse is denoted by a vertex in the graph. At time , each warehouse stores resources of amount , receives a local demand , a local supply of , , sends a partial amount of resources to its out-neighbors in , and receives a partial amount of resources from its in-neighbors in . Let , then the agent follows the dynamics
where is the indicator function that allows distribution of resources only if , is the fraction of resources that agent sends to agent at time , is a constant, is a bounded random quantity , and is a positive scalar. We define the state , action , observation ,and the reward , , where
Appendix I Multi-zone temperature control problem
Consider a building divided into zones. Given a state graph , an observation graph , and a reward graph , the goal of the multi-zone temperature control problem is for the temperature controller in each zone to find an optimal policy that controls a quantity related to the airflow rate and achieves the desired temperature within each zone. Each zone is denoted by a vertex in the graph. Assuming finite thermal resistance of the walls separating adjacent zones, the temperature of each zone is affected by the heat flow from the adjacent zones through walls leading to a dynamic coupling between the zones i.e., , if zone and zone share a common wall, then . The temperature in zone , , evolves according to
where is the temperature in the zone , is the corresponding control input, is the outdoor temperature, is the time resolution, is the thermal capacitance of zone , denotes the thermal resistance of windows and walls between zone and the environment, denotes the thermal resistance between zones , , represents the constant heat addition from external sources into zone , and represents the process noise in the evolution of temperature in zone . Moreover, the controller in each zone observes After each step, each zone receives a reward . Thus, The simulation parameters are summarized in Table 2.
Appendix J Additional results
Effect of on the rate of convergence of MAStAC algorithm
To investigate the effect of on the rate of convergence of the MAStAC algorithm, we run the 40-warehouse example with Fig. 11 shows the comparison of the mean and standard deviation of total reward for various choices of in the ‘MAStAC approximated’ algorithm. We observe that as the value of increases, the rate of convergence decreases which can be attributed to the fact that a higher value of yields a denser